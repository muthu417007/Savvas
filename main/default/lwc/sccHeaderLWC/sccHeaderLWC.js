import { LightningElement, track,api } from 'lwc';

export default class sccHeaderLWC extends LightningElement {
   @track isHeaders = true;

    // Method to handle click event
    handleClick(event) {
        // Handle click logic here
    }
}